import { EnabledClickDirective } from './enabled-click.directive';

describe('EnabledClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EnabledClickDirective();
    expect(directive).toBeTruthy();
  });
});
